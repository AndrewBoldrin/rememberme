import React, { ReactElement } from "react";
import { Index } from "./pages/Index/Index";

const App = (): ReactElement => {
  return (
    <div>
      <Index />
    </div>
  );
};

export default App;
