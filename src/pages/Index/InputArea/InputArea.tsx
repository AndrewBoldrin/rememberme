import React, { ReactElement } from "react";

export const InputArea = (): ReactElement => {
  return (
    <div
      style={{
        width: "50vw",
        height: "100vh",
      }}
    >
      inputarea
    </div>
  );
};
