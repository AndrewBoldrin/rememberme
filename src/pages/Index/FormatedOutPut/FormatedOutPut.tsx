import React, { ReactElement } from "react";

export const FormatedOutPut = (): ReactElement => {
  return (
    <div
      style={{
        width: "48vw",
        height: "100vh",
      }}
    >
      formated output
    </div>
  );
};
